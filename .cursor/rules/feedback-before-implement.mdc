---
description: 意見先對齊再改碼；SAVE 觸發寫入 PM 迭代 log
alwaysApply: true
---

# 意見與改動流程（強制）

> 書面 SOP（給人讀、可交接）：`docs/SOP_AI_COLLABORATION.md`（路徑自專案根目錄）

當使用者提出**意見、問題、想優化的點、或要求修改行為**時：

1. **不要**立刻使用工具改程式碼、改設定或 push。
2. **必須先**用簡短條列說明：
   - **我如何理解你的問題或需求**（重述重點，避免會錯意）。
   - **預計怎麼改**（方向、會動哪些檔案／模組、有無取捨或風險）。
3. 若需求有歧義，**先問一個關鍵澄清問題**再給方案亦可。
4. 僅在下列情況再動手實作：
   - 使用者明確說 **同意、照這樣改、執行、改吧、幫我做** 等；或  
   - 使用者在一則訊息裡**同時**給了需求與執行指令（例如「請直接改」）。

**例外**：純資訊問題（解釋概念、讀檔說明現況）可直接回答，不必先列改動方案。

此規則旨在減少未對齊就改碼、方便產品決策與交接。

---

# 觸發詞「SAVE」（強制）

當使用者訊息**以 `SAVE` 為主要指令**（單獨一行、或明顯為存檔意圖，可附一句補充）時：

1. **必須**更新產品經理迭代檔：`docs/PRODUCT_ITERATION_YYYY-MM.md`（**YYYY-MM 取當月**；若該檔不存在則**新建**最小骨架：標題、§十 SAVE 快照表，或沿用專案內最近一期迭代檔並在檔首註明延用）。
2. 在該檔 **§十、SAVE 快照** 區塊 **新增一列**（或一則條目），至少包含：
   - **日期**（建議 `YYYY-MM-DD`）
   - **摘要**：本次對話重點、決策、共識（精簡，方便日後搜）
   - **待辦／跟進**（可選，條列）
3. 若本次有**可記錄的發布／里程碑**，可**順便**在 `docs/PRODUCT_MANAGEMENT.md` 的「發布紀錄」**補一行**（一句話）；無則省略。
4. **僅 SAVE、未要求改碼時**：**不要**改程式；只更新上述文件（與必要時 `git commit`／`push` 若使用者一併要求）。

**與其他指令並存**：若同一則訊息含 SAVE 與「請實作／改吧」，**先**寫入 SAVE 快照，**再**依一般流程實作（或先對齊再實作，視內容而定）。
