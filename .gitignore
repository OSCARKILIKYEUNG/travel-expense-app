node_modules/
dist/
.env
.env.local

# Google OAuth client secrets (download from console; never commit)
client_secret*.json
.env*.local
*.local
.DS_Store
Thumbs.db

# 本機參考圖（收據截圖等，不進版控）
reference/

# 本機助手工作目錄（不進版控）
.claude/

# 本機打包備份（npm run zip:portable 產出）
portable-zips/
