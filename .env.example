# 僅用於本機執行 `npx vercel dev`（單據 API）。勿把含真鑰的檔案提交 Git。
# Vercel：Settings → Environment Variables，變數名必為 GEMINI_API_KEY（與 Google AI Studio 或 Cloud Console 建立的金鑰皆可，但 Console 須啟用 Generative Language API）。

GEMINI_API_KEY=your_key_here

# Supabase（登入／Email 驗證）；前端需 VITE_ 前綴。專案：Dashboard → Settings → API。
# Vercel 請同步加入 VITE_SUPABASE_URL、VITE_SUPABASE_ANON_KEY。
# Email 驗證：Supabase → Authentication → Providers → Email → 開啟「Confirm email」；
# Authentication → URL Configuration：Site URL 設為本機 http://localhost:3000 與正式網域；Redirect URLs 加入同上。
# 資料同步：在 Supabase SQL Editor 執行 supabase/migrations/001_user_app_data.sql（建立 user_app_data 表與 RLS），登入後才會寫入雲端。
# Google 登入：Supabase → Authentication → Providers → Google（開啟並貼上 Google Cloud OAuth Client ID／Secret）；
# Google Cloud Console → OAuth 用戶端 → 已授權的重新導向 URI 須包含 https://<專案-ref>.supabase.co/auth/v1/callback ；
# Supabase → URL Configuration → Redirect URLs 含本機與正式網域（與 Email 登入相同）。
VITE_SUPABASE_URL=https://xxxx.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_key_here
